name: Perform Mixeway Scan
jobs:
   mixeway-scan:
   name: Mixeway Scanning
   runs-on: ubuntu-latest
   steps:
   - uses: actions/checkout@v1
   - name: Prepare variables
   id: vars
   shell: bash
   run: |
    echo "##[set-output name=branch;]$(echo${GITHUB_REF#refs/heads/})"
    echo "::set-output name=sha_short::$(git rev-parse HEAD)"
   - name: Prepare Mixeway docker image
   run: |
      docker pull mixeway/scanner:latest
   - name: Run Scan
   run: |
       docker run -e OSS_USERNAME=matb2000@wp.pl -e OSS_KEY=ca0b3c5a15b07c068421ec09ab669b0b162fed5e -e MIXEWAY_URL=https://hub.mixeway.io -e MIXEWAY_KEY=65850e8a-a802-4efb-b1a3-79edd5d9bc6d -e  MIXEWAY_PROJECT_NAME=Borko15_lab_secure_app -e MIXEWAY_PROJECT_ID=142 -e MODE=STANDALONE -e BRANCH=master -v /opt/lab_sec_audit/sast:/opt/sources mixeway/scanner:latest
